# Binary (anchor to repo root — bare "diginode-cc" also matched the
# cmd/diginode-cc/ source dir, which made new files there invisible
# to "git add" unless forced).
/diginode-cc

# Frontend build output
web/dist/
web/node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env

# Claude Code (local-only assistant context)
.claude/
CLAUDE.md

# OS
.DS_Store
Thumbs.db
